const fs=require("fs");const c=["import { Router } from \"express\";","import { db } from \"@workspace/db\";","import { eq } from \"drizzle-orm\";","import { campaigns, marketingMessages, quotes, coupons, marketingClientes, marketingMetrics } from \"@workspace/db\";","import { requireAuth, requireRole } from \"../middlewares/auth\";","","const router = Router();","router.use(requireAuth);","router.use(requireRole(\"MARKETING\", \"ADMIN\", \"SUPERUSER\"));","","router.get(\"/campaigns\", async (_req, res) => { try { res.json(await db.select().from(campaigns)); } catch(e){res.status(500).json({error:e.message})} });","router.post(\"/campaigns\", async (req, res) => { try { const d=await db.insert(campaigns).values(req.body).returning(); res.json(d[0]); } catch(e){res.status(500).json({error:e.message})} });","router.get(\"/messages\", async (_req, res) => { try { res.json(await db.select().from(marketingMessages)); } catch(e){res.status(500).json({error:e.message})} });","router.post(\"/messages\", async (req, res) => { try { const d=await db.insert(marketingMessages).values(req.body).returning(); res.json(d[0]); } catch(e){res.status(500).json({error:e.message})} });","router.get(\"/quotes\", async (_req, res) => { try { res.json(await db.select().from(quotes)); } catch(e){res.status(500).json({error:e.message})} });","router.post(\"/quotes\", async (req, res) => { try { const d=await db.insert(quotes).values(req.body).returning(); res.json(d[0]); } catch(e){res.status(500).json({error:e.message})} });","router.patch(\"/quotes/:id\", async (req, res) => { try { const d=await db.update(quotes).set(req.body).where(eq(quotes.id,Number(req.params.id))).returning(); res.json(d[0]); } catch(e){res.status(500).json({error:e.message})} });","router.get(\"/coupons\", async (_req, res) => { try { res.json(await db.select().from(coupons)); } catch(e){res.status(500).json({error:e.message})} });","router.post(\"/coupons\", async (req, res) => { try { const d=await db.insert(coupons).values(req.body).returning(); res.json(d[0]); } catch(e){res.status(500).json({error:e.message})} });","router.get(\"/clientes\", async (_req, res) => { try { res.json(await db.select().from(marketingClientes)); } catch(e){res.status(500).json({error:e.message})} });","router.post(\"/clientes\", async (req, res) => { try { const d=await db.insert(marketingClientes).values(req.body).returning(); res.json(d[0]); } catch(e){res.status(500).json({error:e.message})} });","router.patch(\"/clientes/:id\", async (req, res) => { try { const d=await db.update(marketingClientes).set(req.body).where(eq(marketingClientes.id,Number(req.params.id))).returning(); res.json(d[0]); } catch(e){res.status(500).json({error:e.message})} });","router.get(\"/metrics\", async (_req, res) => { try { res.json(await db.select().from(marketingMetrics)); } catch(e){res.status(500).json({error:e.message})} });","router.post(\"/metrics\", async (req, res) => { try { const d=await db.insert(marketingMetrics).values(req.body).returning(); res.json(d[0]); } catch(e){res.status(500).json({error:e.message})} });","router.patch(\"/metrics/:id\", async (req, res) => { try { const d=await db.update(marketingMetrics).set(req.body).where(eq(marketingMetrics.id,Number(req.params.id))).returning(); res.json(d[0]); } catch(e){res.status(500).json({error:e.message})} });","","export default router;"].join("\n");fs.writeFileSync("src/routes/marketing.ts",c,"utf8");console.log("marketing.ts OK");
